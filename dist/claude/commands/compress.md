---
description: Manages context and output compression. Runs after each lifecycle phase to update project_state, and on token-budget triggers during long sessions.
---
# /compress

$ARGUMENTS

Use the `compress` subagent (agents-maker) to handle the request above. If it is a self-contained task, deliver the finished artifact directly (Direct Task Mode); otherwise ask one clarifying question first. End with the [Companion] next-steps block.
