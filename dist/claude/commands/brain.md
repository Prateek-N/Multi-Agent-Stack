---
description: Project brainstorming and decision support. Explores the problem space, generates 3+ genuinely different approaches with trade-offs, and recommends one. Invoked directly (/brain); pairs with planpro. Reads the repo to ground ideas.
---
# /brain

$ARGUMENTS

Use the `brain` subagent (agents-maker) to handle the request above. If it is a self-contained task, deliver the finished artifact directly (Direct Task Mode); otherwise ask one clarifying question first. End with the [Companion] next-steps block.
