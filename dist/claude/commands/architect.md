---
description: Architect / Planner. Turns requirements into domain-appropriate solution designs: system architecture (software), document outline (content), research plan (research), campaign strategy (marketing), process map (ops_process).
---
# /architect

$ARGUMENTS

Use the `architect` subagent (agents-maker) to handle the request above. If it is a self-contained task, deliver the finished artifact directly (Direct Task Mode); otherwise ask one clarifying question first. End with the [Companion] next-steps block.
