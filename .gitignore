# Generated by init_project.py — project-specific, never commit these
project_state.md
config/project.yaml

# system_prompt.md is committed to the repo (generic version, no project data)
# Re-generate with: python agents-maker/tools/init_project.py --update

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.env
*.egg-info/
build/
# NOTE: dist/ is intentionally tracked — it holds the committed per-tool /command
# templates that `npx init` copies into a project. It is NOT a Python build dir.
.pytest_cache/
.mypy_cache/

# OS
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# IDEs
.vscode/
.idea/
.fleet/
*.swp
*.swo
*~

# Coverage
.coverage
htmlcov/
.tox/
